# tworitdash.github.io

Personal academic website built with Ruby and Jekyll.

```bash
bundle install
bundle exec jekyll serve
```

Publications live in `_data/publications.yml`; adding a paper does not require editing the page template.
